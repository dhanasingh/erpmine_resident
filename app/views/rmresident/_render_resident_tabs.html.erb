<%= javascript_include_tag 'index', :plugin => "erpmine_resident" %>

<script type="text/javascript">
    <% if show_apartment %>
        var apartmentUrl = '<%= url_for(controller: "rmapartment", action: "index", tab: "rmapartment") %>';
    <% end %>
    <% if show_resident %>
        var residentUrl = '<%= url_for(controller: "rmresident", action: "index", tab: "rmresident") %>';
        var incidentUrl = '<%= url_for(controller: "rmincident", action: "index", tab: "rmincident") %>';
        var performserviceUrl = '<%= raw url_for(controller: :rmperformservice, action: :edit, user_id: User.current.id, startday: Date.today, sheet_view: 'I' ) %>';
        var evaluationUrl = '<%= url_for(controller: "rmevaluation", action: "index", tab: "rmevaluation", surveyForType: 'RmResident').html_safe %>';
    <% end %>
</script>

<%= render_tabs resident_tabs %>
<%= hidden_field_tag 'back_url', url_for(:controller => params[:controller], :action => params[:action]) %>