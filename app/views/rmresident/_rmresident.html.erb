<label><%= l(:label_resident_service) %></label>
<div>
	<div class="wk-contextual">
			<%= link_to l(:label_new_res_service), url_for(:controller => 'rmresident', :action => 'newresidentservice', :parentType => currentObj.class.name, :parentId => currentObj.id, :resdient_type => 'RS'), :class => 'icon icon-time-add' %>
	</div>	
	
	<%= render partial: "rmresident/resident_service_list", locals: { resservices: currentObj.blank? ? nil : currentObj.resident_services.includes(:issue).where(issues: { :tracker_id => [1] }), residentType: "RS" } %>
</div>